<?php

    // Podemos contar o número de elementos de um array com a função "count()", use o array como argumento da função e ela retornará um valor inteiro que é o número de itens do array.

    $arr = [1, 2, 3];
    $arr2 = range(1, 100);

    echo count($arr) . "<br>";
    echo count($arr2) . "<br>";

?>