<?php

    // Crie uma operação que utilize subtração (-), divisão (/) e multiplicação (*).
    // Armazene os valores em variáveis e depois imprima-os na tela.

    echo 50 - 1 * 50 / 2; // Operação com todos os operadores pedidos.
    echo "<br>";

    $a = 50; // Todos os valores armazenados em variáveis.
    $b = 1;
    $c = 2;

    $d = $a - $b * $a / $c; // Armazendando o resultado em uma variável e imprimindo na tela.
    echo $d;
    echo "<br>";

?>